# Устарело: сборка перенесена в docker-compose.prod.yml (pull_policy: build).
# Файл оставлен для совместимости — дополнительных секций не добавляет.
#
# Раньше: -f docker-compose.prod.yml -f docker-compose.prod.build.yml
# Сейчас достаточно: docker compose -f docker-compose.prod.yml up -d --build
